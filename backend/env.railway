# Railway Production Environment Variables
# Copy this content to Railway environment variables

NODE_ENV=production
PORT=5000

# Database Configuration (Railway will provide these)
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# JWT Configuration
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=24h

# CORS Configuration (update with your frontend URL)
CORS_ORIGIN=${CORS_ORIGIN}

# Logging
LOG_LEVEL=info

# Security
BCRYPT_ROUNDS=12 